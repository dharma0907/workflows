name: nodejs eks feature
on:
  workflow_call:
    inputs:
      project:
        required: true
        type: string
      component:
        required: true
        type: string
jobs:
  build_nodejs:
   runs-on: ubuntu-latest
   steps:
     - name: calling version
       run: |
         echo "Reading version"
     # we have defined project nad component in github-actions folder and we are calling them here
     - name: reading inputs
       run: |
         echo "project  ${{ inputs.project }}"
         echo "component  ${{ inputs.component }}"
     - name: Checkout Code
       uses: actions/checkout@v7 # this is plugin used for checkout the code

     - name: print the code list files
       run: ls -l
        